# Anthropic API Key (REQUIRED)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# Environment
ENVIRONMENT=development

# CORS Origins
CORS_ORIGINS=http://localhost:3000,https://*.vercel.app

# Example of JSON format if you prefer (valid JSON array)
# CORS_ORIGINS=["http://localhost:3000","https://*.vercel.app"]
